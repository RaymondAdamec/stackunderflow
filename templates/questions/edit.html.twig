{% extends 'base.html.twig' %}

{% block title %}Edit Questions
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Edit Questions</h1>
		<div class="text">
			{{ include('questions/_form.html.twig', {'button_label': 'Update'}) }}
			<a class="btn btn-primary mb-3" href="{{ path('app_questions_index') }}">Back to questions</a>
			{{ include('questions/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
